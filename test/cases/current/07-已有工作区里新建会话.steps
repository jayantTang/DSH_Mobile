# 运行器先把「临时工作区」登记成工作区，于是它出现在弹层的「工作区」一节里；
# 用例在其中新建的会话，跑完连同这个登记一起由运行器删掉。
1  do: 连接本机 DSH  | want: id:session.list
2  do: 点击 id:session.new  | want: id:newSession.root
3  do: 什么都不做  | want: text:工作区, id:newSession.row.dsh-uitest-scratch
4  do: 点击 id:newSession.row.dsh-uitest-scratch
5  do: 等待消失 id:newSession.root timeout=25
6  do: 等待 id:composer.field  | want: id:composer.field
7  do: 打开页面 sessions  | want: id:session.list
8  do: 滚动到 text:dsh-uitest-scratch  | want: label:工作区 /private/tmp/dsh-uitest-scratch | shot: grouped-by-directory
