# TC-MOB-31 的执行指令（失败的工具卡也收起）
#
# 判据：让夹具会话跑一条"必然失败 + stderr 200 行"的命令，失败卡片出现后
#   **正文的第一行与最后一行都不在屏幕上**（收起），并截图留证。
#
# 不验"点开能展开"：这一腿连不上——卡片正文不是可查询的文本（或点到的不是那一张卡），
# 试过点 `row.tool` 与新增的 `tool.header` 都不行。展开那一条留待给正文加标识之后再验，
# 评审里如实写明。
#
# 只用夹具会话（不碰真实会话）：session-fa73530f-9658-4219-86bf-38684d3a9e1d
#   node scripts/dev/image-scratch-session.mjs --dir /tmp/dsh-uitest-draftA
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/31-失败的工具卡也收起.md

1  do: 连接本机 DSH | want: id:session.list
2  do: 滚动到 id:session.ungrouped | want: id:session.ungrouped
3  do: 点击 id:session.ungrouped
4  do: 滚动到 id:session.row.{{session}} | want: id:session.row.{{session}}
5  do: 点击 id:session.row.{{session}}, 等待 id:chat.transcript timeout=25 | want: id:chat.transcript
6  do: 点击 id:composer.field, 等待 1 秒
7  do: 输入 请只做一件事：用 bash 跑这条命令 `for i in $(seq 1 200); do echo \"失败行 $i：这一段用来把失败的输出撑长\" >&2; done; exit 1`，然后只回复两个字：完成。不要做别的分析。 | want: id:composer.send
8  do: 点击 id:composer.send
9  do: 等待 id:row.tool timeout=120 | want: id:row.tool, 不存在 text:失败行 1 ; absent, 不存在 text:失败行 200 ; absent
10 do: 什么都不做 | shot: 01-failed-collapsed check=content,amber
