# TC-CHAT-02 的执行指令（打开长会话落在最底部）
#
# 现象：首次打开会话停在"底部再上一段的中间"，要手动往下拉才到最后；二次打开同样。
#
# 判据：**收尾标记 ZQ7X4P 在屏幕上**。演示会话最后一行只写这个标记，而提示词里刻意把它
# 拆成 `ZQ7` 与 `X4P` 两段说（见 scripts/dev/demo-session.mjs），所以完整标记只可能出现在
# 转写最末尾——"看得见它"等价于"滚到底了"。
#
# 前两版判据都被实测证伪，写在这里避免再犯：
#   * 用「回到底部」浮层按钮不在 → 出现"按钮不在、画面却停在中段"的假通过；
#   * 用「未提交」这类中文短语在不在 → 那句话在转写中段也出现，同样假通过；
#   * 把标记直接写进提示词 → 提示词就在第一屏上，"看得见标记"在停在顶部时也成立。
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/17-打开会话在底部.md

1  do: 连接本机 DSH
# 2 与 3 是同一个动作的两次留证：先拍"刚打开"，再拍"稳定后"。
# 两张逐字节相同 = 打开就是最终位置，没有"先中段再跳到底部"。
2  do: 打开页面 transcript, 等待 id:chat.transcript timeout=10 | shot: 01-opened check=texts,amber
3  do: 什么都不做            | shot: 02-settled check=texts,amber
4  do: 什么都不做            | want: text:ZQ7X4P
5  do: 什么都不做            | want: id:composer.field
# 再打开一次（同一会话第二次进入）——重进也要在底部，不能只在首次生效
6  do: 打开页面 sessions
7  do: 打开页面 transcript   | shot: 03-reopened check=texts,amber
8  do: 什么都不做            | want: text:ZQ7X4P
