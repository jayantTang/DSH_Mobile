# TC-MOB-24 的执行指令（图片类工具卡片默认不显示参数）
#
# 判据：带图片的卡片默认看不到参数 JSON（用 `caption` 这个词做反向核对——本会话里它只
# 可能来自那张卡片被 pretty-print 出来的参数），但「显示参数」按钮在；点开后 JSON 回来、
# 按钮变成「隐藏参数」；再点一次又收起来。
#
# 为什么能点到按钮却不先滚动：会话转写打开时落在最底部，而最近的图片卡片就在那里。
# 点击能否成功本身就是「按钮可见」的证据——引擎里 `滚动到` 只要元素进了树就算过，
# 用它证明"看得见"是假的（第一次跑就是这么被骗过去的）。
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/24-图片工具卡片不显示参数.md

1  do: 打开页面 transcript | want: id:chat.transcript
2  do: 什么都不做 | want: 不存在 text:caption ; absent
3  do: 点击 text:显示参数, 等待 text:隐藏参数 timeout=20 | want: text:隐藏参数
4  do: 什么都不做 | want: text:caption
5  do: 什么都不做 | shot: 01-arguments-shown check=texts,amber
6  do: 点击 text:隐藏参数, 等待 text:显示参数 timeout=20 | want: text:显示参数
7  do: 什么都不做 | want: 不存在 text:caption ; absent
8  do: 什么都不做 | shot: 02-arguments-hidden check=texts,amber
