# 临时会话由运行器的「临时目录」创建；{{session}} 就是它。
# 一个 want 里的多项用逗号分隔，`; absent` 表示「不该出现」。
1  do: 连接本机 DSH  | want: id:session.list
2  do: 滚动到 id:session.ungrouped  | want: id:session.ungrouped
3  do: 点击 id:session.ungrouped  | want: id:session.row.{{session}}
4  do: 滚动 left id:session.row.{{session}}  | shot: swiped check=texts after
5  do: 点击 id:session.archive.{{session}}
6  do: 什么都不做  | want: id:session.row.{{session}} ; absent, id:session.archive.banner | shot: archived check=texts after
