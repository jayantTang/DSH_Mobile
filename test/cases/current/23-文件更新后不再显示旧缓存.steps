# TC-MOB-23 的执行指令（下载过的文件在电脑上更新后不再显示旧的）
#
# 判据：第一次打开给出精确字节数 300000（新下的那份）；第二次打开之后的那行会写
# 「已按电脑上的新版本重新读取」——说明 revalidate 认出了版本变化、丢弃旧副本重下。
# 快链路上刷新过程只有一瞬，所以断言落在完成态而不是「正在重新读取」那句上。
#
# 版本变化由 DEBUG 钩子注入：`-DSHSimulateRemoteChange,附件.bin,8`，让开局 8 秒之后、
# 对「已经下过一份」的文件的那次打开看到不同 version；丢弃旧副本、重新下载、界面文案
# 都是真实路径。（钩子带时间条件：SwiftUI 在分支切换时会重跑 .task，只按调用次数
# 计数会在第一次冷开时就误触发。）
#
# 跑法：test/fixtures/git-demo/setup.sh && DSH_CONNECT=relay ./test/run.sh run test/cases/current/23-文件更新后不再显示旧缓存.md

1  do: 打开页面 files args="-DSHSimulateRemoteChange,附件.bin,8" | want: id:files.root
2  do: 点击 id:files.entry.附件.bin, 等待 text:已可分享 timeout=60 | want: text:300000
3  do: 什么都不做 | shot: 01-first-download check=texts,amber
4  do: 点击 text:完成, 等待 8 秒, 等待 id:files.root timeout=20
# 「电脑上的文件已更新，正在重新读取…」是刷新**过程中**那句；300 KB 在快链路上几十毫秒
# 就下完了，所以完成态的断言落在「已按电脑上的新版本重新读取」上。
5  do: 点击 id:files.entry.附件.bin, 等待 text:已可分享 timeout=60 | want: text:300000
6  do: 什么都不做 | want: text:已按电脑上的新版本重新读取
7  do: 什么都不做 | shot: 02-refreshed check=texts,amber

# 钩子已经用掉：第三次打开必须是一次普通的缓存命中——不再重下，那句「已按新版本重新读取」
# 也该消失。没有这两步，即使「每次打开都重下」这条用例照样会绿。
8  do: 点击 text:完成, 等待 2 秒, 等待 id:files.root timeout=20
9  do: 点击 id:files.entry.附件.bin, 等待 text:已可分享 timeout=60 | want: text:300000, text:已按电脑上的新版本重新读取 ; absent
