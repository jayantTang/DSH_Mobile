# TC-MOB-30 的执行指令（三种状态：蓝点运行中 / 绿点完成未读 / 无点已读）
#
# 判据：
#   * 列表里存在 id:session.state.running 与「运行中」（执行本用例的会话本身在跑）；
#   * 往夹具会话里发一轮，跑完后展开未分组、滚到它，出现 id:session.state.finishedUnseen 与「已完成」；
#   * 进它一次再退回列表，两个文案都不在（已读＝不画点）。
# 颜色本身不可自动判（整屏启发式判不了单行颜色），看三张截图。
#
# 只用夹具会话（不碰真实会话）：session-fa73530f-9658-4219-86bf-38684d3a9e1d
#   node scripts/dev/image-scratch-session.mjs --dir /tmp/dsh-uitest-draftA
#
# 两条固定坑：`滚动到` 只往下滚；**重开页面之后未分组是收起的**，要先点开再滚。
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/30-三种状态的标识.md

1  do: 连接本机 DSH | want: id:session.list
2  do: 什么都不做 | want: id:session.state.running, text:运行中
3  do: 什么都不做 | shot: 01-running-blue check=content,amber
4  do: 滚动到 id:session.ungrouped | want: id:session.ungrouped
5  do: 点击 id:session.ungrouped
6  do: 滚动到 id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d | want: id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d
7  do: 点击 id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d, 等待 id:chat.transcript timeout=25 | want: id:chat.transcript
8  do: 点击 id:composer.field, 等待 1 秒
9  do: 输入 请只回复两个字：收到 | want: id:composer.send
10 do: 点击 id:composer.send
11 do: 点击 text:返回, 等待 id:session.list timeout=20
12 do: 等待 25 秒
13 do: 打开页面 sessions | want: id:session.list
14 do: 滚动到 id:session.ungrouped | want: id:session.ungrouped
15 do: 点击 id:session.ungrouped
16 do: 滚动到 id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d | want: id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d
17 do: 什么都不做 | want: id:session.state.finishedUnseen, text:已完成
18 do: 什么都不做 | shot: 02-unseen-green check=content,amber
19 do: 点击 id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d, 等待 id:chat.transcript timeout=25
20 do: 点击 text:返回, 等待 id:session.list timeout=20
21 do: 滚动到 id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d | want: id:session.row.session-fa73530f-9658-4219-86bf-38684d3a9e1d, 不存在 text:已完成 ; absent, 不存在 text:运行中 ; absent | shot: 03-read-no-dot check=content,amber
