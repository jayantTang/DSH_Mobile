# TC-DEMO-01 的执行指令（与人读的用例文件分开放）
#
# 一行一步，对应 14-宣传配图合成会话.md 里的第 N 步。
# 用语表与页面名见 01-全部独立页面逐屏核对.steps 与 test/screens.md。
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/14-宣传配图合成会话.md
# 走中转通道不只是为了让设置页出现「已配对设备」——配图里的连接方式得和产品一致。

1  do: 连接本机 DSH          | shot: 01-sessions check=texts,amber
2  do: 什么都不做            | want: label:连接管理, label:设置
3  do: 打开页面 transcript   | shot: 02-transcript check=texts,amber
4  do: 什么都不做            | want: id:chat.transcript
# 第 5 步只为核对：文件页确实落在合成会话的工作区上（`-DSHOpenSession` 生效），
# 截图本身不入 README。
5  do: 打开页面 files        | shot: 03-files check=texts,amber
# 设置页要 `-DSHDemoMode`：它把中转地址与主机主目录换成占位符（SettingsView.masked）。
# 真实域名进了配图等于公开部署地址。
6  do: 打开页面 settings args=-DSHDemoMode | shot: 04-settings check=texts,amber
#
# 已知做不到的三件事（都不是这条用例的问题，是控件与模拟器的限制）：
#   * 转写页**滚不动**。`滚动 down` 让引擎给 chat.transcript 发滑动，模拟器抛
#     "Pointer events are not supported for this device"——进程级异常，整轮直接 ERROR。
#     先试 scroll(byDeltaY:) 也一样抛（试过，已回退）。所以这张图停在首屏：
#     用户提问 + 工具卡片，看不到最终答复。
#   * 「浏览 / 变更」是 SwiftUI 分段控件：两个选项各有 label，点击事件也发了，
#     但界面不切换（`label:变更` 与按比例点 70%,11% 都试过），所以文件页只拍「浏览」态。
#   * 会话列表里合成会话排在真实会话下面，所以那张图只作核对，不进 README。
