# 运行器先在本用例的「临时目录」里建一个会话，于是该目录出现在弹层的
# 「其他项目目录」里；用例在其中新建的会话，跑完由运行器一并删除。
1  do: 连接本机 DSH  | want: id:session.list
2  do: 点击 id:session.new  | want: id:newSession.root
3  do: 什么都不做  | want: text:其他项目目录
4  do: 滚动到 id:newSession.row.dsh-uitest-scratch  | want: id:newSession.row.dsh-uitest-scratch
5  do: 点击 id:newSession.row.dsh-uitest-scratch
6  do: 等待消失 id:newSession.root timeout=25
7  do: 等待 id:composer.field  | want: id:composer.field
8  do: 什么都不做  | shot: inside-new-session
9  do: 打开页面 sessions  | want: id:session.list
10 do: 滚动到 text:dsh-uitest-scratch  | want: label:工作区 /private/tmp/dsh-uitest-scratch, text:dsh-uitest-scratch | shot: grouped-by-directory
