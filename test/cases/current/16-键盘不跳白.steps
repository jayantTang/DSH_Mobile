# TC-CHAT-01 的执行指令（流式输出时弹键盘/打字，会话不跳白）
#
# 用户报的两个触发点：打开键盘、打字。真正会跳白的组合是「**有内容在流入 + 键盘弹起**」：
# 流式输出每次追加都会请求跟到底部（scrollSignal），键盘同时在改视口高度，
# LazyVStack 的可见区间被挪到还没渲染的空位上 —— 画面一片空白，往下拉才恢复。
#
# 所以这条用例不是"静止的会话里点一下输入框"（那样几百次也不复现），而是：
# 发一条会让 host 真跑一轮的消息 → 键盘保持弹出 → 流式过程中打字 → 逐帧核对会话还在。
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/16-键盘不跳白.md

1  do: 连接本机 DSH, 打开页面 transcript
# 键盘弹出：视口缩短 —— 空白最容易出现在这一刻
2  do: 点击 id:composer.field | shot: 01-keyboard-up check=texts,amber after
3  do: 什么都不做            | want: id:chat.transcript
# 明确不要用工具：默认的 coding agent 会去 ask_user_question，那一轮就变成"等你回答"，
# 不是纯流式输出。这条用例要的是"内容持续流入"这个条件。
4  do: 输入 直接回答，不要调用任何工具：把 1 到 60 用逗号连成一行
5  do: 点击 id:composer.send
# 「已发送，等待电脑端开始…」消失 = host 已经开始这一轮，流式输出开始
6  do: 等待消失 text:已发送，等待电脑端开始… timeout=60 | shot: 02-streaming check=texts,amber after
7  do: 什么都不做            | want: id:chat.transcript
# 流式过程中再打几个字：这是用户描述的"打字时变白"
8  do: 输入 打字中         | shot: 03-typing-during-stream check=texts,amber after
9  do: 什么都不做            | want: id:composer.field
# 跑完的标志：这条"正在运行"的状态点消失（转写里的「思考过程」是每行的折叠标签，不会消失）
10 do: 等待消失 id:chat.running timeout=180 | shot: 04-after-turn check=texts,amber after
