# 运行器先在本用例的「临时目录」建一个空会话（{{session}}），跑完连同它一起删除。
# 这条用例验的是「重进会话会不会多出一份回答」，所以要真的有一条回答——
# 往临时会话里发一句最省事的话，等它答完，再退回列表重进两次。
1  do: 连接本机 DSH  | want: id:session.list
2  do: 滚动到 id:session.ungrouped  | want: id:session.ungrouped
3  do: 点击 id:session.ungrouped  | want: id:session.row.{{session}}
4  do: 点击 id:session.row.{{session}}  | want: id:chat.transcript
5  do: 点击 id:composer.field
6  do: 输入 请只回复四个字：我收到了  | want: id:composer.send
7  do: 点击 id:composer.send
8  do: 等待 text:我收到了 timeout=120  | shot: reply-first after
9  do: 等待消失 id:composer.stop timeout=120  | want: id:composer.send
10 do: 什么都不做  | shot: first-visit
11 do: 点击 text:返回  | want: id:session.list
12 do: 滚动到 id:session.row.{{session}}
13 do: 点击 id:session.row.{{session}}  | want: id:chat.transcript
14 do: 什么都不做  | shot: second-visit
15 do: 点击 text:返回  | want: id:session.list
16 do: 点击 id:session.row.{{session}}  | want: id:chat.transcript
17 do: 什么都不做  | want: text:我收到了 | shot: third-visit
