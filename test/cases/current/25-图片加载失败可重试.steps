# TC-MOB-25 的执行指令（加载失败的图片可以重试）
#
# 判据：失败态出现「点按重试」按钮；点它之后图片出现（id:attachment.image），
# 且「点按重试」消失（absent 反向核对）。
#
# 失败由 `-DSHFailAttachmentLoad,1` 注入：每个附件的第一次请求失败；之后的下载、
# 解码、渲染都是真实路径。
#
# 为什么用专门的图片会话：转写打开时落在最底部，而工作会话的底部是最近的 bash 卡片
# ——第一版用例往上滚六屏也没找到图片卡片。`image-scratch-session.mjs` 造的会话里
# 只有一条带图片的消息，图片就在屏幕内，不需要滚动、也不靠运气。
#
# 跑法：node scripts/dev/image-scratch-session.mjs   （把打印的 id 填进用例的 `会话:`）
#       DSH_CONNECT=relay ./test/run.sh run test/cases/current/25-图片加载失败可重试.md

1  do: 打开页面 transcript args="-DSHFailAttachmentLoad,1" | want: id:chat.transcript
2  do: 什么都不做 | want: id:attachment.image.retry, text:点按重试
3  do: 什么都不做 | shot: 01-failed check=content,amber
4  do: 点击 id:attachment.image.retry, 等待 id:attachment.image timeout=45 | want: id:attachment.image
5  do: 什么都不做 | want: 不存在 text:点按重试 ; absent
6  do: 什么都不做 | shot: 02-reloaded check=content,amber

# 关于像素检查：这两张用 `content` + `amber`。`texts` 量的是"深色墨迹 ≥ 0.20%"，而这张
# 屏幕的文案是 labelTertiary 浅灰配 codeBackground 浅底（实测 0.00%），`brand` 要求主色就是
# 品牌蓝（实测主色 87% 白，Δ=178）——两个 heuristic 都是给深色/控件密集的屏幕设计的。
# 为此给测试台加了 `content`：够不够多像素与主色不同（≥2%），问的是"到底画出东西没有"，
# 这正是浅色极简屏幕上该问的问题。
