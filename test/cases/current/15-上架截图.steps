# TC-DEMO-02 的执行指令（App Store 上架截图）
#
# 与 TC-DEMO-01 的区别：那一条拍的是 README 用的压缩图（460×1000），这一条拍**上架用**
# 的原图（1206×2622，即 iPhone 17 Pro 的 6.9" 规格），并且：
#   * 不入库（App Store 的素材是发布产物，不进仓库）；
#   * 只拍三个不涉及真实会话数据的页面——会话列表那张会把真实项目名拍进去；
#   * 走中转通道 + `-DSHDemoMode`，与 README 那两张同一套脱敏。
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/15-上架截图.md

1  do: 连接本机 DSH
2  do: 打开页面 transcript   | shot: 01-transcript check=texts,amber
3  do: 打开页面 files        | shot: 02-files check=texts,amber
4  do: 打开页面 settings args=-DSHDemoMode | shot: 03-settings check=texts,amber
# 连接页与配对表单：审核员要看的"怎么连上"，也不含真实数据。
# 连接页里有电脑名与中转地址（对外信息），同样要脱敏。
5  do: 打开页面 connections args=-DSHDemoMode | shot: 04-connections check=texts,amber
