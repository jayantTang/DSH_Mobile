# TC-MOB-26 的执行指令（运行中的行排在最前）
#
# 判据：发一轮之后退回列表，那一行**还在屏幕内**（排序的结果：运行中的组被排到最前，
# 它本来是排在后面的），并且列表里有「运行中」。
#
# 排序规则与三态判定不在这里断言，由单测逐条覆盖：
#   DSHKit/Tests/DSHKitTests/SessionListOrderTests.swift（组与行的顺序）
#   DSHKit/Tests/DSHKitTests/SessionRowStateTests.swift（运行中/未查看/已查看/空会话）
# 原因写在用例的「覆盖与不覆盖」里：列表的样子与顺序无关的渲染一模一样，只有正好抓到
# 移动的那两行才分得出顺序；这台机器的列表里还永远有一条在跑的会话（执行用例的那条），
# 拿「运行中」文案或「等它消失」做断言都会指向错的那一行。
#
# 跑法：DSH_CONNECT=relay ./test/run.sh run test/cases/current/26-运行中的分组排最前.md

1  do: 连接本机 DSH | want: id:session.list
2  do: 滚动到 id:session.ungrouped | want: id:session.ungrouped
5  do: 点击 id:session.ungrouped
6  do: 滚动到 id:session.row.{{session}} | want: id:session.row.{{session}}
5  do: 点击 id:session.row.{{session}}, 等待 id:chat.transcript timeout=25 | want: id:chat.transcript
6  do: 点击 id:composer.field
7  do: 输入 请只回复两个字：收到 | want: id:composer.send
8  do: 点击 id:composer.send
9  do: 点击 text:返回, 等待 id:session.list timeout=20
10  do: 什么都不做 | want: id:session.row.{{session}}, id:session.state.running, text:运行中
11  do: 什么都不做 | shot: 01-running check=content,amber
