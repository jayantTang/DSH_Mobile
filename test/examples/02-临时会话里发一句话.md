---
id: TC-EXAMPLE-02
title: 示例：在运行器新建的临时会话里发一句话
用例版本: 1
通过判据: 8 步结论全为通过；2 张标注截图看过；像素核对通过
临时目录: /tmp/dsh-example-scratch
备注: 模板演示"要写入会话的用例必须声明会话归属"：写 `临时目录:` 让运行器新建会话，跑完自动清理；不要用运行器挑选的真实会话，也不要写死会话 id（除非那是一个专门的夹具）。
---

# 示例：临时会话里发一句话

1. 连接本机 DSH
2. 滚到「未分组」
3. 展开未分组
4. 滚到运行器新建的临时会话
5. 进入它的转写
6. 点输入框并输入一句话
7. 发送并确认消息出现在转写里
8. 截图留证
