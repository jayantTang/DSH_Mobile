---
id: TC-EXAMPLE-01
title: 示例：连接后会话列表存在，并截一张图
用例版本: 1
通过判据: 4 步结论全为通过；1 张标注截图看过；像素核对通过
---

# 示例：只读检查

不写入任何会话的最小用例：连接、确认列表在、截图、回到顶部。
把它当作模板复制到 `test/cases/current/` 后改名（编号 + 标题），并写同名的 `.steps`。

## 步骤

1. 连接本机 DSH
2. 确认会话列表存在
3. 截图留证
4. 确认「会话」标题仍在屏幕上
