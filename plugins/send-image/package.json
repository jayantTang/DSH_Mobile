{
  "name": "dsh-plugin-send-image",
  "version": "0.1.0",
  "private": true,
  "description": "Registers the send_image tool: the agent puts a picture (a screenshot, the clipboard, or a file) into the conversation, where every client displays it and the user can open it full screen.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml"
  ],
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
